using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStateMachine : MonoBehaviour
{
    
    // switch cases for movement:
    
    // IDLE -> crouch -> sneak
    //      -> move   -> run    -> jump
    //                -> jump        

    void MovePlayer(string inputKey)
    {
        switch (inputKey)
        {
            
                
        }
    }
}
